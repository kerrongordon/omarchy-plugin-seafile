# Never anything credential-shaped, even if a future refactor moves the
# account store back under the plugin directory by accident.
account.ini
*.token
*.secret

# Regenerated fresh on every test run by tests/extract_py.js.
/tests/_extracted/
__pycache__/
*.pyc
